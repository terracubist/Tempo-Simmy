import pandas as pd
import numpy as np
import random
import functools
from scipy.stats import norm
from scipy.stats import bootstrap
from scipy import special as sp
import networkx as nx
import math
import timeit
import code

print(f"pd.version is: {pd.__version__}")
print(f"np.version is: {np.__version__}")
print(f"random choice of [1, 2] is {random.choice([1, 2])}")
print(f"help(norm) is: {help(norm)}")
print(f"nx.version is: {nx.__version__}")
print(f"math.log10(10) is: {math.log10(10)}")
